const positionTableData = [];

const positionRowData = {
  position: "Camera 1 (Video)",
  time: "9 am - 7 pm",
  info: "default",
  quantity: "20",
};

for (let i = 0; i < 8; i++) {
  positionTableData.push(positionRowData);
}

export default positionTableData;
